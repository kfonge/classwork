
// method for area //// note: in area function, {} are not necessary, b/c its only one line of code

const area = (radius) => { return Math.PI * radius ** 2 }


//method for circumference

const circ = (radius) =>  2 * Math.PI * radius 

module.exports = { circ, area}

